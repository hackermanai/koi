
[[./koi.png]]

* Koi Editor

[[https://github.com/hackermanai/koi/issues][Submit feature request or report bug]]

[[https://koieditor.com/EULA][End User License Agreement]]

[[https://koieditor.com/PRIVACY][Privacy Policy]]

** How to use inline shell and chat

Inline commands can be triggered with /Inline Commands/ (or ~Cmd + Shift + Return~).

*Shell and Python eval*

This feature is only allowed in Generic-Default, ~.txt~, ~.md~, ~.org~ files by default.
You can change allowed files in your ~.hackerman~ config file.

#+begin_src
% echo "hello koi!"
hello koi!

% ollama list
NAME                              ID              SIZE      MODIFIED      
qwen3-next:80b-cloud              f5ccd68d2872    -         3 weeks ago      
qwen2.5-coder:1.5b                6d3abb8d2d53    986 MB    12 months ago    
#+end_src

Evaluate Python expression.

#+begin_src
42**2
1764
#+end_src

*Chat with LLMs*

This feature requires setting up Ollama in your ~.hackerman~ config file

#+begin_src
[models]

code_completion                     qwen2.5-coder:1.5b
inline_chat                         qwen3-coder:480b-cloud
#+end_src

Move cursor to line/ or select text and /Submit Prompt/ (or ~Cmd + K~) to generate response.

#+begin_src
write fizz buzz in reverse using basic

> Here's FizzBuzz in reverse using BASIC:
>
> 10 FOR I = 100 TO 1 STEP -1
> 20 IF I MOD 15 = 0 THEN PRINT "FizzBuzz"
> 30 ELSE IF I MOD 3 = 0 THEN PRINT "Fizz"
> 40 ELSE IF I MOD 5 = 0 THEN PRINT "Buzz"
> 50 ELSE PRINT I
> 60 NEXT I
>
> This counts down from 100 to 1, printing:
> - "FizzBuzz" for multiples of 15
> - "Fizz" for multiples of 3
> - "Buzz" for multiples of 5
> - The number itself otherwise
#+end_src


** How to create custom editor commands

This feature is disabled by default.
You can enable in your ~.hackerman~ config file.

#+begin_src
scripts_enabled                     true
allow_unsafe_scripts                false -- allow imports
#+end_src

User scripts are loaded from ~.../Application Support/Koi/.hackerman-scripts~.
You can provide a path to your own ~.py~ file if stored elsewhere.

#+begin_src
path_to_scripts_file                path/to/plugins.py
#+end_src


** Other tips and tricks

- Right click to /Show Command Palette/ (or ~Cmd + Shift + P~), this list shows all editor commands and key bindings
- Double click on line number to /Fold Line/ (or ~Cmd + L~)
- Double click on word to select, ~Cmd~ plus double click on another word to add to selection
- Select text then /Select Next Match/ (or ~Cmd + E~) to select next exact match, /Unselect Next Match/ (or ~Cmd + Shift + E~) to unselect last added match
- Find with /Find In File/ (or ~Cmd + F~), ~Esc~, then /Select Matches/ to select/add cursor to each match
- Select brace to highlight open and/or closing brace
- /Show File Explorer/ (or ~Cmd + P~) shows all files and folder on Desktop by default, ~my-project/~ to quickly find files in specific folders
